<p id="notice"><%= notice %></p>

<% if @flat.photo.attached? %>
<%= cl_image_tag @flat.photo.key, width: 300, height: 300, crop: :fill %>
<% end %>
<p>
  <strong>Address:</strong>
  <%= @flat.address %>
</p>

<p>
  <strong>Flat Type:</strong>
  <%= @flat.flat_type %>
</p>

<%= link_to 'Edit', edit_flat_path(@flat) %> |
<%= link_to 'Back', flats_path %>